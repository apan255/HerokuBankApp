<%= form_for((@friend), :url => { :action => "create" }, :html => { :method => :post }) do |f| %>
  <% if @friend.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@friend.errors.count, "error") %> prohibited this friend from being saved:</h2>

      <ul>
      <% @friend.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    Enter email id
    <%= f.text_field :email %>
  </div>
    <div class="field">
      Enter name
      <%= f.text_field :name %>
    </div>
  <div class="actions">
    <%= f.submit "Add Friend"%>
  </div>
<% end %>
