json.extract! transaction, :id, :amount, :trans_type, :status, :trans_from_id, :trans_to_id, :start_date, :end_date, :from_account_id, :to_account_id, :created_at, :updated_at
json.url transaction_url(transaction, format: :json)